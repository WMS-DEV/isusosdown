import styled from "styled-components";
import {GlobalMediaQueries} from "../../../../../assets/globalMediaQueries";

export const CsvText = styled.text`
  color: white;
  display: inline-flex;
  align-items: center;
  cursor: pointer;
`

export const DownloadImg = styled.img`
  height: 2vh;
  margin-left: 1vh;
  aspect-ratio: 1/1;
`